# Bump notes

## 2026-08-29 — Wheels V1.4 visual polish

CURRENT VER=0.0.0-seed

PENDING VER=0.1.0-alpha.0

- Reconnected every prominent Wheels action to the Public semantic button system, with primary/secondary/ghost/compact/text/danger/danger-outline hierarchy across the landing hero, `/wheels/new`, editor, Appearance, Import / Export, participant manager, access, and winner result surfaces. Build/Create/Save are gold; navigation and discard stay dark; destructive winner actions are visibly graduated.
- Re-centred the route-driven Wheel Editor on a viewport-fixed layer with bounded `dvh` height and mobile-safe padding, aligned the creator action bar, and completed the Public / Approved / Official information rail with an explicit lower border.
- Expanded the finite celebration to bounded 44/96/148-particle tiers with varied shapes, fall and side-cannon paths, theme-derived colour, warm bloom, crimson/violet fans, voltage passes, and a decaying rim. Reduced motion now renders no falling confetti or travelling beams and retains a static winner halo.
- Added Wheels V1.4 semantic-source and responsive browser coverage for CTA hierarchy, action variants, information-rail termination, viewport modal centring, winner action hierarchy, bounded particles, light layers, cleanup, reduced motion, and desktop/mobile screenshots. Wheel authority, official selection, persistence, portable formats, media, D1, R2, permissions, and routes are unchanged.
- Deployed Admin first as `d079d3f0` (`https://d079d3f0.thirdrailify-admin.pages.dev`) and Public as `38f5077f` (`https://38f5077f.thirdrailify.pages.dev`). Stable V1.1-V1.4 browser acceptance passed, including the anonymous creator-access CTA, complete information rail, finite demo celebration, cleanup, reduced-motion treatment, clean console, no overflow, and zero non-read API requests.

- Added Wheels V1.3 portable creator files: deterministic `thirdrailify-wheel` v1 `.twl` and equivalent JSON/copy exports, SHA-256 wheel-payload integrity, bounded optional same-origin embedded media, and explicit exclusion of owner/access/history/lock/revision/R2/session/security authority.
- Added content-detected `.twl`, canonical/generic JSON and Wheel of Names `.wheel` import with strict UTF-8/size/depth/key/array/string/media limits, fresh local entry IDs, one-at-a-time multi-config selection, complete preview/conversion warnings, and local dirty editor state that persists only through existing Save/Create permission and revision contracts.
- Added the served v1 JSON Schema, `WHEELS_FILE_FORMAT.md`, a sanitized eight-entry Wheel of Names fixture, deterministic round-trip/conversion/security/media tests, an Import / Export sibling dialog in the existing editor, and the `/wheels/new` import path. No Admin/API/schema/migration/D1/R2 authority was added or changed.

## 2026-08-29 — Wheels V1.2 in-context control and participant odds

CURRENT VER=0.0.0-seed

PENDING VER=0.1.0-alpha.0

- Replaced the existing-wheel full-page editor presentation with a route-driven, responsive lightbox over the mounted wheel while retaining direct `/wheels/:slug/edit`, reload, browser-Back, clean close, authorization, revision-safe Save/Discard, dirty-close confirmation, and recoverable save-error behavior. `/wheels/new` remains the dedicated unsaved creation route.
- Added the dedicated participant-management dialog with quick/bulk add, search/order/sort, Web Crypto shuffle, reverse, hide/unhide, remove/confirmed clear, weights, colours, duplicate warnings, bounds, and explicit revision-protected Save/Discard. Appearance remains a sibling preview-first dialog so modal editors never stack.
- Added shared weighted angle/pointer/click geometry, the connected pointer-target HUD in detail and presentation modes, rotation-aware medallion/exterior-safe Canvas selection, keyboard participant-row selection, and a persistent desktop/mobile participant detail surface. Current odds are derived only as entry weight over total active weight; exact duplicate labels additionally show their combined active chance without exposing private metadata or invented history.
- Added the reusable exact-path `trzap-0.svg` gold-gradient mark and replaced placeholder Wheels hero, loading/empty, card, default-centre, and winner motifs. Presentation Exit and Fullscreen controls now keep each icon and label aligned on one line.
- Added deterministic geometry/odds tests and expanded Playwright coverage for route/modal state, unauthorized edit, dirty close, responsive editor/manager/details/HUD, list and Canvas selection, visible exact zap paths, delayed loading, presentation alignment, no overflow, and clean console output.
- Deployed only the existing Public Pages project as production/main staging deployment `ce4d7c7b-9803-4ecd-a261-5f71b3de62f3` (`https://ce4d7c7b.thirdrailify.pages.dev`). Stable and immutable served the same `index-CHBvZ_x_.js` application asset. Stable browser acceptance proved the exact zap in landing/loading/wheel/winner states, the pointer HUD, matching row/Canvas participant details, authoritative `18.18%` odds for weight 2 of total 11, protected unauthenticated `/edit` over the mounted wheel, demo-spin celebration/audio cleanup, no `/api/` mutation during V1.2 inspection, no overflow, clean stable console, and inline-aligned Exit/Fullscreen icon-label pairs at 1920x1080, 1280x720, and 390x844. Authorized editor and participant-manager modal acceptance remained fixture-backed because bypassing the remote account boundary is prohibited.

## 2026-08-29 — Wheels V1.1, Community navigation, and VIP preview

CURRENT VER=0.0.0-seed

PENDING VER=0.1.0-alpha.0

- Upgraded wheel detail/presentation with the centred `SPIN WHEEL` console, compact mode/sound/Appearance controls, layered illuminated rim, custom-background stage treatment, local gold `trzap-0.svg` centre without `TR`, finite full-screen confetti/lighting, accessible result dialog, and gesture-unlocked generated winner stinger with cleanup and independent settings.
- Added six bounded palettes, per-entry colour picker/hex/reset workflow, save/discard Appearance tabs, Admin-authoritative PNG/JPG/BMP/WebP/safe-SVG background and centre ingestion, opaque hash-safe delivery, hidden-wheel protection, R2 replacement/removal cleanup, additive `0016` D1 metadata, audit, and rate limits. Public still owns no Wheels D1 or R2 binding.
- Added semantic Community hover/focus/mobile children, top-level VIP, one reusable VIP feature in the requested Community/Home positions, and a complete truthful `/vip` scaffold with billing, purchase, and entitlements explicitly inactive.
- Added focused migration/media/security/browser/audio/responsive evidence. No official visual-test draw, payment/provider write, bot/Wix/DNS/domain change, secret change, or production cutover was performed.
- Applied empty additive D1 migration `0016` after a 3,013,256-byte backup and invariant check; deployed Admin `afd9db50` then Public `511d5421`. Stable Pages browser/audio acceptance passes. Remote media rows remain zero because no authenticated owner/editor test session was available and the upload boundary was not bypassed.

## 2026-08-29 — Customer checkout and shipping foundation

- Added the real `/checkout` route and cart handoff with an accessible, responsive, non-persistent delivery form, explicit server quote request, safe rate selection, server-projected CAD subtotal/shipping/total, quote invalidation after cart/address changes, and a canonical-gate-disabled payment action.
- Added bounded exact-origin same-origin quote/checkout relays. Public receives no Commerce D1 binding, provider credential, Printful mapping, raw rate response, or authoritative price input; recipient details never enter localStorage.
- Added focused Node 22.16.0 proxy/source and Playwright coverage at 1440, 768, and 390 pixels for validation, autofill semantics, disabled/unavailable and mocked-rate states, totals, invalidation, closed checkout, privacy, keyboard focus, reduced motion, overflow, and clean console behavior.
- No real Printful/Stripe/provider request, customer order, payment, email, remote D1 mutation, configuration change, deployment, or commerce activation occurred.

## 2026-08-28 — Commerce-authoritative replacement shop

- Preserved historical paid status for the first genuine Stripe TEST acceptance after the separate Admin-controlled creation gate was closed. The success redirect remains read-only, normal Public checkout remains disabled, and no second Session/order can be created from Public.
- Added the truthful `/checkout/success` TEST payment result: processing while the signed webhook is pending, confirmed only from local payment authority, and not-confirmed for invalid/unknown correlation. The browser never calls Stripe or trusts the redirect query as payment proof.
- Added a no-store same-origin order-status proxy keyed only by an opaque `cs_test_…` Session ID. Its bounded output contains payment/order state, safe order reference, CAD amount, and disabled fulfillment—never provider secrets, PaymentIntent, account IDs, audit data, or Printful mappings.
- Normal Public checkout remains disabled. The one pre-cutover Session can be generated only from the authenticated Master Admin acceptance card; fulfillment and the independently paused Printful migration remain untouched.
- Switched `/shop` and homepage merchandising from the eight-product Wix runtime snapshot to the sanitized Commerce D1 projection through a same-origin Pages Function proxy.
- Added permanent `/shop/:slug` details, real variant selection, variant-specific CAD display prices, quantity, and a `{ productId, variantId, quantity }` local cart.
- Added truthful catalogue failure/retry states with no hard-coded fallback and a customer-safe disabled-checkout message.
- Kept the Public project free of Commerce D1 bindings, provider IDs, Admin credentials, and provider implementation details.

CURRENT VER=0.0.0-seed

PENDING VER=0.1.0-alpha.0

No prior version metadata or release scheme existed in this repository. `0.0.0-seed` names the pre-scaffold local state; `0.1.0-alpha.0` establishes the first pending, non-production V2 milestone.

## Pending 0.1.0-alpha.0

### Technical

- Added Third Railify Wheels V1: first-class `/wheels`, public detail, approved creator new/edit, and clean presentation routes; a high-DPI weighted Canvas renderer; Web Crypto demo selection and shuffle; explicit practice versus server-recorded official mode; deterministic stops; generated tick/stinger audio; bounded gold/crimson light and confetti celebration; accessible result focus/live-region behavior; winner keep/hide/remove actions; public history; responsive navigation; and focused engine/gateway/browser coverage. Public remains free of the wheel/Commerce D1 binding and adds no browser storage, payment, fulfilment, bot, Wix, DNS, or provider write.

- Replaced the dormant homepage newsletter scaffold with a similarly sized gold direct-contact band containing real Contact and `/donate` actions. Contact opens a responsive focus-contained dialog with Escape/backdrop dismissal, focus restoration, bounded fields, honeypot, explicit Privacy acknowledgement, fail-closed Turnstile, sending/error/success states, and no recipient or provider authority in the browser. A bounded same-origin Public relay forwards only JSON plus a signed, one-way per-client rate token to the Admin authority; Admin verifies that relay proof, enforces exact Public origin and D1-backed rate limiting, validates the `thirdrailify-contact` Turnstile action and fields, and sends through Resend to `info@thirdrailify.com` with `thirdmailify@gmail.com` copied and the visitor email as Reply-To. No raw client IP crosses in form content, and no newsletter, marketing subscription, Public Resend key, mailbox record, secret change, or new Cloudflare resource was created.
- Rebuilt the `/watch/episodes` hero after a broad legacy hero selector forced the archive feature card's timestamp and nested metrics into oversized display typography. The hero now uses a cohesive motion-gated signal field, drifting light, rails, orbit geometry, scan depth, and a compact telemetry footer; the feature card is a balanced control-room register with a restrained capacity gauge, paired visible/open metrics, a full-width readable latest-episode timestamp, and a 24-node activity rail. Responsive coverage now explicitly rejects the former stacked date treatment, oversized metadata, unbalanced desktop card proportions, reduced-motion regressions, and overflow without changing Watch ingest, archive authority, retention, ordering, visibility, IDs, or episode data.
- Refined Shop V2 around a single gallery price authority: `/shop`, collection galleries, featured/related cards, the cart drawer, and `/cart` now show emphasized gold CAD amounts with local Canadian SVG flags and no gallery currency selector or converted sub-price. Product details alone retain a compact inline comparison row with a keyboard-accessible, locally flagged currency chooser; CAD stays primary, and variant plus quantity remain one row through 390px. Added the dedicated CartProvider-backed `/cart`, drawer `View full cart`, and query/hash-preserving `/cart-page` redirect. Public collection discovery now consumes Admin's ordered visible collection projection rather than reconstructing configuration from category strings.
- Replaced the ambiguous `/support` migration shell with a complete first-class `/donate` destination. The page now has a cinematic, motion-safe community-power hero; signal scope, rails, waveform, and status treatment; accessible one-time/monthly/yearly cadence controls; CAD preset/custom amount preparation; truthful donation-purpose and no-benefit disclaimers; and a visibly disabled PayPal handoff that creates no request, checkout, agreement, or transaction. Footer and homepage navigation now say `Donate`; `/support` and `/donate-1` preserve query/hash while redirecting to `/donate`. Added responsive browser coverage for 1440×900, 768×1024, and 390×844, keyboard controls, disabled payment state, route aliases, one H1, reduced motion, and overflow.
- Added end-to-end animated GIF support for optional GOAT profile media while keeping main/gallery uploads limited to JPG, PNG, and WebP. Profile GIFs bypass browser canvas flattening, retain native animation after Admin structural sanitization, and render through ordinary image elements. Listings with missing or failed profile media now show a sleek CSS-drawn goat silhouette on cards and detail identities, and the existing desktop GOATS hero motif is nudged 7% left without disturbing tablet/mobile placement. Focused Function and browser fixtures cover role restrictions, two retained GIF frames, native rendering, fallback semantics, identity sizing, and overflow.
- Refined GOAT detail identities and engagement controls after live visual review: display names now scale to the available editorial column, long names receive a compact two-line treatment, and the slightly enlarged profile image aligns exactly with the title row. Reactions are compact accessible thumb controls whose selected icons fill lime/red while retaining authoritative counts; Copy link now has a copy glyph; and the empty state reads `No comments yet.` Added desktop/mobile browser coverage for alignment, long-name height, selected icon fill, control density, wording, and overflow.
- Added the minimal-disruption Public privacy layer: a compact non-modal bottom dock with equally accessible one-click Accept all and Reject non-essential actions, a focused Manage sheet, a global footer Privacy choices control, and a host-only version-1 consent cookie with a 183-day lifetime.
- Derived only two optional categories from the real inventory. Preferences gates currency/rate-cache and non-sensitive GOATS draft persistence and cleans those values on withdrawal; External media prevents YouTube privacy-enhanced and Rumble iframe creation until opt-in, supports a media-only grant, and unmounts active players when withdrawn. Essential auth/security, cart, consent-choice storage, navigation, and direct provider links remain available.
- Documented the exact browser/provider inventory and updated the Privacy Policy. Cloudflare Web Analytics is absent from the top-level staging page; the pre-change beacon observation was frame-attributed to the eager Rumble player and is now behind External media consent. No Analytics or Marketing category was invented.
- Deployed only the existing Public Pages project as deployment `50645ae2-e67d-4b79-97ff-a1fca2c1706c` (`50645ae2.thirdrailify.pages.dev`). Clean stable/immutable acceptance proved the first layer, equal one-click decisions, reject/accept persistence, granular withdrawal, zero pre-consent provider requests/iframes, real post-consent Rumble activation, essential APIs, footer reopening, reduced motion, and overflow-free 1920×1080, 1440×900, 1024×768, 768×1024, and 390×844 layouts.

- Rebuilt the GOATS gallery/detail presentation after live review exposed three regressions: primary card photos were accidentally trapped behind their fallback layer until hover created a new stacking context, Windows native option menus inherited an unreadable light palette, and the rigid detail media box letterboxed photographs inside oversized blank bands. Primary media now permanently occupies the card surface above a true error-only fallback; filters and option popups request and supply a dark palette; cards use a denser editorial composition; and detail photographs cover a viewport-scale responsive media canvas with the uncropped original still available in the lightbox. The hero no longer renders an arbitrary three-image montage, adds decorative `LAX` and `LHR` signals to the retained `SYD`/`YYZ` radar, and layers a sourced public-domain mountain-goat line illustration behind the copy as a theme-coloured CSS vector mask. No raster artwork or reused map-pin SVG appears in the hero.
- Deployed the isolated GOATS-only artifact as production Pages deployment `1cbc215e-ffd3-4f55-b83d-a0c448659b39` (`1cbc215e.thirdrailify.pages.dev`). Stable served byte-identical HTML referencing `index-oDp55Sd1.js` and `index-Bg43MfqO.css`; 1440×900 and 390×844 browser acceptance both reported MapLibre `ready`, eleven features, real successful OpenFreeMap PBFs, working Sydney/Toronto selection, pan/zoom, dark controls, permanent pre-hover card photography, viewport-filling detail media, and no overflow or application errors. The final desktop/mobile hero, gallery, detail, and map captures were opened and visually inspected after capture.
- Replaced the five legal migration shells with a first-class V2 policy system: a graphical `/policies` register and deep-linked `/terms`, `/privacy`, `/refunds`, and `/accessibility` documents, all driven by one future-extensible typed registry. Each dense document has one H1, stable section anchors, priority jump cards, a complete on-page contents rail, current-document navigation, revision metadata, readable long-form typography, direct contact paths, and responsive layouts down to 390px.
- Reworked the supplied `Legal+Disclaimers.csv` as source content rather than runtime authority. Removed Wix/Wix Payments/Wix POS and co-branded-service phrasing; corrected the obsolete ephemeral-only Watch claim for the real bounded 24-record archive; disclosed actual Cloudflare hosting/Functions/Workers/D1/Durable Object/R2/Turnstile, eight-hour session, local cart/currency/draft, approximate-location GOATS, Discord, video embed, map, identity, and conditional commerce boundaries; and added purpose-based retention, European lawful-basis/rights/transfer language, and a WCAG 2.2 AA goal without an unaudited conformance claim.
- Corrected the entire legal-content baseline to Third Railify's confirmed Canadian business scope and owner-confirmed London, Ontario operating base. Terms and Privacy identify the legal owner only by his established public professional name, `Shawn`, because publishing his surname would create a security risk for on-screen talent. Privacy uses the Ontario ordinary-private-sector PIPEDA baseline and OPC-aligned accountability, purpose, meaningful-consent, limiting, safeguards, openness, access/correction, withdrawal, and complaint explanations; no entity type, corporation number, tax registration, privacy officer, street address, representative, transfer safeguard, or return period was invented.
- Completed the CASL implementation audit: Resend code sends account/security and GOATS submission/moderation service messages, prepared commerce templates are inactive, and the visible newsletter control remains disabled. No operational promotional-email feature was found, so no fictional marketing-consent or unsubscribe system was added; activation remains a review gate.
- Completed the Competition Bureau pricing review against the current catalogue projection: CAD remains authoritative, optional foreign-currency amounts remain visibly approximate, normal checkout is disabled, and no sale/compare-at field or mandatory application fee was found. Terms now require attainable enabled-checkout pricing, upfront mandatory-charge disclosure, and support for sale/comparison claims before activation.
- Added `LEGAL_RELEASE_CHECKLIST.md` with explicit CONFIRMED, NOT APPLICABLE, NEEDS OWNER CONFIRMATION, and NEEDS LEGAL REVIEW statuses. Ontario is confirmed; remaining gates include the exact contracting form, any legally required service address, privacy-accountability contact, Ontario checkout review, sales footprint, voluntary returns, provider processing, retention, and EU/UK scope review.
- Deployed only the isolated Canadian legal Public artifact as Pages production deployment `acf59157-2f67-4308-9c8a-0653319450ce` (`https://acf59157.thirdrailify.pages.dev`), deliberately excluding concurrent Public/Admin GOATS avatar/GIF work and leaving Admin undeployed. Stable and immutable 1440×900/390×844 browser acceptance returned HTTP 200 for `/privacy`, `/terms`, `/refunds`, and `/policies`, proved one H1, working contents links and Privacy Choices, no horizontal overflow, and no prohibited legal framing. Stable-origin console acceptance was clean; the immutable hostname retains the expected Admin auth-config CORS boundary and was tested strictly for same-origin HTTP/page errors. Storefront, Watch, GOATS, and their three primary APIs returned 200 on both origins.
- Validated under Node 22.16.0 with lint, typecheck, 49 Function tests, 32 Watch tests, 12 GOATS tests, 6 storefront tests, the consent browser test, 3 policy/content/browser tests, exact-zero KV mutation gate, state budget, production build, diff hygiene, prohibited-copy scan, and a zero-hit changed-file high-confidence secret scan. The unrelated pre-existing state fingerprint mismatch remains `expected=fa8f5d87d30e64f6` versus `declared=8bacb98aa3992f26` (diagnostic exit 2); this legal-content work did not alter or rewrite its protected state architecture.
- Added a deliberately non-interactive future-membership register card with no fake route or terms, a reusable policy maintenance document, footer Refund navigation, and dedicated Playwright coverage for all five routes at 1440×900, 768×1024, and 390×844. The fixture verifies one H1, no header collision or horizontal overflow, readable body copy, complete table-of-contents anchors, current-document semantics, four real policy cards, and the non-interactive future slot.

- Completed the eleven-feature stable-map acceptance after the imported dataset exposed near-coincident coarse coordinates in Sydney and Toronto. MapLibre and the Leaflet recovery engine now apply deterministic screen-space separation to pins that resolve to the same two-decimal-degree display cell, leaving stored privacy coordinates unchanged while keeping every marker individually clickable. Deployment `11b0ab87` serves the fresh `index-BcKFAwCf.js` artifact; stable desktop 1440×900 and mobile 390×844 both reported MapLibre `ready`, eleven markers, real successful OpenFreeMap vector tiles, selectable Sydney/Toronto/imported Daniel markers, working pan/zoom/expansion, no fallback/errors/overflow, and visually inspected dark-geography proof captures.
- Published the owner-supplied Wix `WildGoats` collection through the existing Admin authority: nine approved legacy stories now join the two retained demos for eleven map/listing features, with all supplied imagery served from private Admin R2 through approved-only media responses. Public receives no uploader email, Wix owner ID, object key, or moderation metadata. Authentic Wix aggregate like/dislike totals are retained without fabricating member reaction identities.
- Added United States to the same local small rectangular SVG flag system used by Canada and Australia. Imported US cards, detail identities, map popups, selected panels, and accessible location selectors therefore keep the approved rectangular flag treatment; the hero diagram remains intentionally airport-code-only.
- Public detail interactions now honor the Admin-resolved per-listing/global policy. Disabled controls are clearly unavailable, auto-approved comments/reactions behave normally, and approval-required writes show a truthful pending notice without optimistic public-count inflation.

- Rebuilt `/watch/episodes` as a premium archive signal system with a layered grid/frequency field, motion-gated rails and orbit geometry, a truthful 24-node retained-capacity gauge, a four-column real-first gallery, the user-facing `PAST EPISODES` / `EPISODE ARCHIVE` terminology, and a closing CTA to the exact `https://rumble.com/thirdrailify` destination. The collection no longer calls itself “transmission slots”; placeholders are non-interactive archive positions.
- Rebalanced `/watch` to exactly five featured archive positions. The newest five visible historical candidates are selected after excluding the current player candidate by immutable provider/content key, and truthful placeholders fill the remainder without changing episode IDs, order, routes, retention, visibility, ingest, or SQLite authority.
- Corrected that rail to six featured archive positions after desktop acceptance exposed a vacant sixth CSS grid track. The selector and rendered drawer now both use six positions, allowing a real sixth historical episode or a truthful placeholder to fill the established six-column presentation without changing Watch data/state authority.
- Added a fail-soft same-origin `/api/catalogue/banner` projection and shared SiteShell banner renderer. Normal Admin-configured static/ticker/crossfade messages yield automatically to a deep-crimson Live Now takeover only for a non-stale, unexpired Watch `liveNow` candidate; the real title comes from the shared BroadcastProvider and the action is fixed to `/watch/live`. Reduced motion presents stable content, the staging/Wix truth rail remains separate, and the Pages-to-Pages fetch deliberately uses normal redirect handling.
- Added browser/function coverage for five/24 archive positions, duplicate-current exclusion, terminology, exact Rumble CTA, banner normalization/failure, normal effects, reduced motion, genuine-live/upcoming/stale/disabled priority, no-banner state, and restoration when live ends. The full required Public matrix passed under Node 22.16.0: lint, typecheck, 32 Watch tests, 46 Function tests, 11 GOATS tests, 4 storefront tests, the responsive Watch browser suite, exact-zero KV mutation ban, state budget, unchanged state fingerprint `8bacb98aa3992f26`, production build, diff hygiene, and changed-file secret scanning.
- Deployed the fresh Public artifact second, after the coupled Admin authority, to existing Pages production/staging deployment `2a789a55-a318-4e64-9e0f-b8accf4518e4` (`2a789a55.thirdrailify.pages.dev`). Stable-origin acceptance at 1440×900 and 390×844 proved five Watch archive positions, 24 episode archive positions, no legacy “transmission slots” label, the exact safe Rumble CTA, and no horizontal overflow. Controlled routing against the deployed bundle proved configured text in static/ticker/crossfade modes, the disabled no-banner state, and a fresh verified-live fixture rendering the real-title crimson takeover with its fixed `/watch/live` action, without changing remote configuration or Watch history. The real remote Watch snapshot was delayed/offline and therefore correctly showed no false live takeover; its archive held 3 visible records and 21 truthful placeholders. Stable Public and immutable deployment URLs, Admin/Public banner projections, and protected Admin rejection were all healthy.

- Superseded the emergency Leaflet-as-primary presentation below after owner review rejected the lower-detail raster result as the permanent GOATS experience. Restored pinned MapLibre GL 6.6.0 as the normal renderer over OpenFreeMap's live dark vector style, with a Vite-emitted same-origin module worker, a real TileJSON/vector-tile probe, rendered-basemap-feature readiness, compact direct `goatpin.svg` DOM markers, and no arbitrary success timeout. Pinned Leaflet 1.9.4 remains only as an automatic secondary engine when the vector source or WebGL is genuinely unavailable; the text list appears only if both interactive engines fail.
- Preserved and completed the presentation work while restoring vector GL: the animated worldwide signal hero, dark Wix-inspired listing popups, selected-listing synchronisation, modal-scale expansion, Escape/body-scroll handling, responsive resize/recentering, compact pins, pan/zoom, attribution, and desktop/mobile layout all remain. Added local Australian and Canadian SVG flag assets plus a reusable unknown-country SVG fallback, and prefixed every GOATS map-card location, selected location, gallery location, detail location, shared-point label, and fallback selector with the matching small rectangular flag. Narrowed the gallery/detail avatar selectors so flags cannot inherit circular 48/76-pixel profile-image styling. The popup's approved-signal eyebrow remains unflagged, and the animated hero diagram intentionally uses only the uncrowded `SYD`/`YYZ` airport-code chips. Replaced the hero's rotating conic aurora—which moved an angular seam/kink through the viewport—with a blurred, edge-to-edge linear sheen whose slow translated sweep preserves the ambient motion without converging geometry.
- Matched the two-line GOATS hero heading to the established public-hero typography rhythm by increasing its line-height from `.75` to `.82`, removing the visual collision between “GOATS” and “IN THE WILD” at desktop and mobile sizes without changing the type scale.
- Fixed the exact MapLibre/Vite integration defect found during restoration and then caught its production-build-only variant before acceptance: MapLibre 6.6's ESM build derives a sibling worker that Vite development prebundling did not serve, while a plain `?url` production import copied only the worker entry and left its generated `maplibre-gl-shared.mjs` dependency absent. Pages rewrote that missing module request to the HTML shell, so the live style and `.pbf` both returned 200 but the worker could not parse tiles and readiness correctly stayed at zero. The map now imports the module through Vite's `?worker&url` pipeline, calls `setWorkerUrl()` before construction, excludes MapLibre from development prebundling, and emits one self-contained fingerprinted same-origin worker covered by the existing `worker-src 'self' blob:` policy.
- Replaced the raster-primary browser assertions with real-network vector acceptance requiring `maplibre`, two marker features, a non-zero GL canvas, at least one successful OpenFreeMap planet `.pbf`, rendered basemap features, both named pins, dark in-bounds flagged popups, listing/marker selection in both directions, pan/zoom, expansion, animated hero, scrolling, and zero horizontal overflow. Separate fault-injection cases prove automatic Leaflet recovery after vector failure and the accessible list only after both vector and raster sources fail.
- Deployed the final self-contained-worker, rectangular-flag, airport-code-only hero-chip, cleaned linear-sheen, and corrected hero-spacing artifact to existing Pages production/staging deployment `dfb26411-4b06-4cc8-b58f-52916d0883cc` (`dfb26411.thirdrailify.pages.dev`); the stable alias served byte-identical HTML referencing `index-DrpBj9YI.js` and `index-DFn1mJ8v.css`. Stable-origin desktop 1440×900 and mobile 390×844 acceptance both reported MapLibre `ready`, two markers, non-zero rendered vector features, successful live OpenFreeMap planet PBF responses, selectable Sydney/Toronto markers, one small rectangular dark-popup flag beside the location only, expansion, and no fallback/errors/overflow. The final captures `%TEMP%\thirdrailify-goats-hero-spacing-desktop-PROOF.png`, `%TEMP%\thirdrailify-goats-hero-spacing-mobile-PROOF.png`, `%TEMP%\thirdrailify-goats-gallery-rect-flags-PROOF.png`, `%TEMP%\thirdrailify-goats-detail-rect-flag-PROOF.png`, `%TEMP%\thirdrailify-goats-map-desktop-PROOF.png`, and `%TEMP%\thirdrailify-goats-map-mobile-PROOF.png` were each opened and visually inspected after capture; both hero proofs show separated title lines with the public-hero `.82` line-height, the hero visibly has code-only `SYD`/`YYZ` chips and no moving angular kink, the retained flags are rectangular, and both map proofs contain real dark vector geography, controls, a compact pin, and the selected flagged listing card.

- Superseded the earlier false-positive GOATS map acceptance after fresh stable-origin desktop/mobile evidence and a new isolated-browser reproduction proved `thirdrailify.pages.dev/goats` still entered its text-only fallback. WebGL and MapLibre initialization were healthy, but the deployed build consumed the Wrangler-provided OpenFreeMap vector style, that style never emitted `load`, and the component's hard 12-second timer permanently rendered “Map view is unavailable.” The earlier test falsely treated a local one-pixel mocked tile plus MapLibre canvas palette/DOM-pin checks as equivalent to real stable-origin geography and did not require explicit engine/feature/tile readiness attributes.
- Removed MapLibre 6.6.0 from the normal GOATS path and replaced it with pinned integrated Leaflet 1.9.4 plus dark-treated OpenFreeMap Natural Earth raster tiles, required OpenFreeMap/OpenMapTiles/OSM attribution, compact direct `goatpin.svg` DOM markers, no-wrap/bounded geography, multi-feature fit, selection recentering, responsive invalidation, and React cleanup. An initial CARTO candidate was rejected during post-capture image inspection because its nominal HTTP-200 tiles visibly said “API KEY REQUIRED”; no keyed provider remained. Machine-verifiable readiness now requires a live map, non-zero viewport, at least one successfully loaded real raster tile, and a marker for every valid approved map feature; the accessible list appears only after Leaflet reports that every initial tile failed, with no arbitrary timeout.
- Restored the richer Wix-era discovery affordance without restoring its iframe architecture: every integrated Leaflet pin now opens a branded dark listing card with approved-signal metadata, image/fallback art, approximate location, bounded excerpt, and direct story action. Marker click still selects the matching side panel, while listing focus recenters and opens the same card. Added a real modal-scale map expansion control with scroll lock, Escape/close handling, responsive Leaflet invalidation, unchanged live tile/marker state, and a machine-readable expanded flag.
- Rebuilt the previously static GOATS hero as a motion-gated global signal composition with an animated coordinate grid, scanning light field, radar sweep, concentric telemetry rings, pulsing Sydney/Toronto nodes, live approved totals, and responsive/reduced-motion layouts. Browser coverage now rejects a missing animated hero visual, non-dark marker card, broken card link/media, or map expansion that does not materially increase the viewport.
- Strengthened GOATS browser coverage to use current Vite source and real network tiles, require `ready`/`leaflet`/feature-count/tile-count state, successful authoritative API and OpenFreeMap raster responses, two visible named markers, nonblank composed geography, Sydney/Toronto marker-to-listing selection, listing-to-marker selection, real drag pan and zoom-control scale change, tile-failure fallback, and zero overflow or map/CSP/application errors at desktop and mobile sizes.
- Deployed the final dark-map artifact to existing Pages production/staging deployment `ffa8a74b-bbf2-4c0b-b8c9-c3cd558a87d3` (`ffa8a74b.thirdrailify.pages.dev`); stable `thirdrailify.pages.dev` served the identical JS/CSS hashes. Stable-origin desktop 1440×900 and mobile 390×844 acceptance reported Leaflet `ready`, two features, successful real `tiles.openfreemap.org/natural_earth/ne2sr` HTTP 200 responses, both Sydney/Toronto markers selectable, working pan/zoom, mobile scrolling, no fallback/errors/overflow, and dark visible geography with compact pins. Both `%TEMP%\thirdrailify-goats-map-desktop-PROOF.png` and `%TEMP%\thirdrailify-goats-map-mobile-PROOF.png` were opened and visually inspected after capture; both contain real geography and both city markers.
- Force-fixed the deployed GOATS map after browser reproduction exposed a coupled client failure: the redundant full-world `maxBounds` crashed MapLibre's initial projection, the default selection moved the camera to zoom 5 before the initial style load, the remote vector style never scheduled its tiles, the custom resize observer prevented a stable render lifecycle, and MapLibre 6.6's GeoJSON worker never completed the GOATS source. Replaced the fragile default with a directly verifiable OpenFreeMap Natural Earth raster style, deferred selection movement until readiness, restored MapLibre's built-in resize path, and moved approved pins to accessible screen-space-clustered DOM markers using the existing branded SVG. CSP remains limited to the OpenFreeMap tile origin and MapLibre blob worker. Added a real Playwright regression that requires tile requests, completed readiness, nonblank geography, visible lime pins, no fallback/CSP errors, and no horizontal overflow against both deterministic and live tiles.
- Deployed the corrected Public bundle only as Pages deployment `183ab6e7-8e45-40ee-adb0-bf5e54608f3a` (`183ab6e7.thirdrailify.pages.dev`). Canonical `.pages.dev` acceptance returned `200` from the Admin authority and all Public GOATS/merchandising projections; desktop 1440×1000 and mobile 390×844 both requested real OpenFreeMap tiles and rendered two branded pins. Desktop marker selection updated the selected listing and recentered the chosen pin. Both viewports had completed map readiness, no fallback, GOATS/CSP/console errors, relevant network failures, or horizontal overflow. Admin, secrets, D1, R2, demo data, Wix, DNS, custom domains, and production-domain routing were unchanged.
- Completed the Watch V2 final presentation pass without changing Watch authority: `/watch` now uses a cinematic integrated player/metadata stage, explicit live/upcoming/latest/delayed/stale/unavailable signal language, an accessible segmented provider selector, and a cohesive six-position archive rail that includes all newest visible retained episodes.
- Rebuilt the 24-position episode archive around a compact hero and adjacent summary toolbar, four-column desktop density, deterministic signal-waiting placeholder variants, and clearly actionable real media cards. Dedicated live and retained-detail routes gained focused theatre framing, archive context, and card-like previous/next navigation; all motion is transform/opacity based and reduced-motion gated.
- Corrected the Watch browser fixture regression at its source: the suite had launched `vite preview` against potentially stale `dist`, so it now serves current source while retaining the exact episode-heading assertion. The fixture now exercises one valid retained card plus truthful placeholders, and the responsive matrix covers 1920, 1440, 1365, 1024, 768, and 390-pixel layouts as applicable.
- Validated under Node 22.16.0 with lint, typecheck, Watch/Functions/GOATS/storefront tests, Watch browser tests, KV mutation ban, state budget, state fingerprint, production build, diff hygiene, and changed-file secret scanning. The state fingerprint remains `8bacb98aa3992f26`; no Watch API, archive selection/order/retention/visibility, state Worker, KV, ingest, provider validation, bot, secret, or synthetic history path changed.
- Deployed only the existing Public Pages project as deployment `f8fa35ce-82ed-423d-92ac-f7029af4a17a` (`f8fa35ce.thirdrailify.pages.dev`). Stable-origin Playwright acceptance at 1440×900 and 390×844 returned `200` with one H1, no overflow or page-origin console errors on Watch/live/archive/real-detail routes, one current player, six drawer slots (2 real/4 waiting), and 24 archive slots (2 real/22 waiting). The real current delayed Rumble episode and both naturally retained records remained intact.
- Fixed the Admin Watch authority read boundary after production tracing proved the public visible-only APIs were healthy while the protected management request was rejected with `401 invalid_signature` because the two encrypted `THIRDRAILIFY_COMMUNITY_API_SECRET` bindings had drifted. The owner restored one matching value in both Pages projects; no secret was exposed, logged, or placed in browser code.
- Added a signed bodyless `GET /api/watch/manage` contract for hidden-inclusive Admin reads while leaving signed JSON `POST` visibility mutations unchanged. The signature covers the exact method/path and empty-body digest, unsigned/expired/invalid requests still fail closed, hidden records remain absent from public enumeration, and current-snapshot failure no longer erases a successfully read archive.
- Deployed the fresh Public Functions bundle after secret synchronization as Pages deployment `5a014429` (`5a014429.thirdrailify.pages.dev`). Stable acceptance returned `200` for `/api/watch` and `/api/watch/episodes`, `401 invalid_signature` for unsigned management, rendered the latest selected Watch transmission, and preserved both naturally retained visible Rumble episodes with 22 truthful placeholders and no synthetic history.
- Activated the two fixed opt-in staging GOATS demos (`DEMO-GOAT-01` Midnight Rail and `DEMO-GOAT-02` Southern Signal) with the existing idempotent seed. They use coarse synthetic coordinates, contain no real personal information or media, remain removable by the unchanged cleanup script, and are not migration/startup fixtures.
- Added Watch V2 on the existing SQLite-backed `thirdrailify-public-state` Durable Object: current `broadcast` state and the distinct versioned `broadcast_archive` record are committed atomically, visibility mutations serialize with ingest, unchanged snapshots avoid archive rewrites, and hidden records remain inside the deterministic 24-record retention cap.
- Archive population consumes only the canonical candidate in the already-validated signed Watch snapshot. Completed/published `episode` or `archive` presentations are eligible; upcoming, live, blocked, unknown, and transient absence are not. Stable public IDs hash immutable platform/content identity, metadata upserts preserve visibility, and no provider API, search, RSS, HTML scrape, synthetic backfill, D1, R2, KV write, new binding, or new secret was introduced.
- Added visible-only episode list/detail APIs, historical Rumble thumbnail proxying, `/watch`, `/watch/live`, `/watch/episodes`, `/watch/v/:episodeId`, and effective-live-only `/live` redirect semantics. The redesigned Public surfaces render six featured and 24 gallery positions with branded non-clickable placeholders for hidden/unfilled slots and no fabricated episode data.
- Added the Master-only Admin Watch workspace with current/archive summaries, per-record and bulk visibility actions, deliberate bulk-hide confirmation, loading/failure/empty/save states, and public preview links. Admin validates session, role, origin, CSRF, body, and rate limit before signing the protected Public management request with the existing shared secret; Public verifies HMAC/replay bounds, mutates the Durable Object, and Admin writes the action through its existing audit path.
- Updated state source fingerprint/schema/release and conservative write/storage budgets, added no-scrape and state/API/security/route/placeholder/responsive browser coverage, and documented the architecture in `WATCH_V2.md`. The remote archive initially returned zero records, then naturally retained the existing real Rumble episode when the next signed publisher snapshot arrived; no synthetic Watch history was inserted.
- Deployed the existing state Worker as version `50a3dedc-dbe7-47e5-a96e-214606109f37`, the isolated Watch-only Admin artifact as Pages deployment `a0463117-ce34-423b-a3d8-0ffe636eac8e`, and Public as `7310be4f-4594-4de1-bb2b-19b15a18a6aa`. Stable `.pages.dev` acceptance confirmed state contract v2/fingerprint `55aa4326fa05d14c`, current Watch regression, one naturally ingested real episode/23 public placeholders, effective-offline `/live`, protected management rejection, and both GOATS demos. Wix, DNS, custom domains, and production routing were untouched.

- Fixed the deployed Public-to-Admin GOATS proxy failure caused by the GOATS-only `redirect: "error"` subrequest option being rejected by the Cloudflare Pages runtime before an upstream response existed. GOATS now uses the same proven normal abort-bounded fetch pattern as merchandising while preserving fixed Admin URL construction, methods, safe headers, response/cache/media forwarding, HMAC-signed writes, and generic transport failures.
- Added regression coverage that reproduces the Pages redirect-mode rejection and verifies real empty GET projection forwarding, query strings, HEAD, safe upstream 4xx/5xx responses, malformed Admin origins, timeout sanitisation, approved-media headers, and unchanged server-side write signing. Staging acceptance confirms the Admin authority and Public projections are healthy without adding Public D1/R2 authority or applying demo data.
- Deployed the corrected Public build only to the existing Pages Production/main staging project as `92dd58e8-260e-4160-a441-6c93d6793d91` (`92dd58e8.thirdrailify.pages.dev`). Stable `.pages.dev` acceptance returned `200` for listings, map, config, products, and merchandising; browser checks rendered the legitimate empty map/gallery state without API errors, console errors, or horizontal overflow. Admin remained on its healthy existing deployment and no custom domain, DNS, Wix, secret, D1, R2, or seed mutation occurred.
- Built the complete local V2 GOATS in the Wild surface at `/goats`, `/goats/submit`, and `/goats/:slug`, with `/goatgate` preserving query/hash into the submission route and route precedence preventing `submit` from becoming a dynamic slug. Added premium approved-only discovery, URL filters/country facets/pagination, detail media/lightbox/product navigation, authenticated reaction/comment controls, and real product-detail submission CTAs without changing pricing, cart, or checkout.
- Replaced the Wix/GitHub iframe dependency with route-lazy pinned MapLibre GL 6.6.0, clustered minimal GeoJSON, custom tracked `goatpin.svg`, real cluster expansion/counts, disabled world copies and wheel trapping, bounded controls, selected-card synchronisation, and WebGL/map-failure list fallback. The optional non-secret style URL is provider-neutral; the blank fallback embeds no Mapbox token or public demo tile dependency.
- Configured the Public GOATS MapLibre staging/default style with the non-secret OpenFreeMap dark style URL while preserving the existing environment override and basemap-free fallback behavior.
- Added the fixed same-origin `/api/goats/*` boundary for approved projections, media, staged guest submission uploads/finalisation, authenticated reactions, and authenticated comments. Public uses existing session/CSRF identity, strict origin/method/body bounds, privacy-HMAC rate keys, bounded Admin timeouts, generic failures, and a server-only shared signature; it owns no D1, R2, email, moderation, or product authority.
- Added client orientation normalisation, 2,400-pixel metadata-free display derivatives, accessible upload/review progress, approximate city-only privacy language, Turnstile fail-closed behaviour, consent, private browser-draft exclusions, and server-validated product-ID preselection. Production starts empty; the later owner-supplied Wix export has no runtime dependency here, while exactly two synthetic records remain opt-in Admin local/test fixtures only.
- Added focused GOATS Function/security tests and documentation for routing, proxy allowlists/outage errors, upload bounds, map clustering/fallback/world-copy settings, canonical product IDs, and environment contracts. No D1/R2/provider/email/deployment/domain/Wix mutation was performed.
- Refreshed the deterministic state-backend source fingerprint because its protected inventory includes Public `_routes.json`; the Durable Object, SQLite snapshot, producer, migration, and zero-KV-write behaviour are unchanged.

- Rebuilt `/shop` and every existing product-detail route into a premium responsive merchandise drop: deterministic featured rotation with pause/previous/next, visibility/hover/focus/reduced-motion gates, graphical live-count category discovery, URL-backed filters/search/sort, redesigned product cards, truthful failure states, preserved semantic links, related products, and unchanged local-cart identifiers/behavior.
- Added one shared USD-default approximate currency system across the hero, catalogue, product details, and related cards. The same-origin `/api/currency-rates` Function validates the configured HTTPS CAD upstream, positive three-letter rates and reference date, adds CAD=1, uses a 4.5-second timeout and cache/stale headers, while the browser persists a namespaced selection/cache and leaves authoritative CAD/cart values untouched on every failure path.
- Added the fail-soft `/api/catalogue/merchandising` proxy and catalogue-provider overlay so Public consumes only Admin-owned featured ID/slug/order metadata. Missing or unavailable Admin projection deterministically falls back to displayable local snapshot products; no commerce D1 binding or privileged write was added to Public.
- Added focused storefront commerce tests for rate normalization/rejection, CAD base, USD default, valid/invalid preferences, USD/AUD/JPY conversion, missing prices, upstream failure, cache headers, and merchandising projection bounds. Updated Function routing, Wrangler safe rate URL, README architecture, and the repository tree; no deployment or provider/domain mutation occurred.
- Refreshed the deterministic state-backend source fingerprint because its protected inventory includes Public `_routes.json` and `wrangler.jsonc`; no Durable Object, SQLite snapshot, ingest, or legacy-KV behavior changed.
- Validated with Node 22.16.0 lint/typecheck, 40 combined Function/state/storefront tests, exact-zero KV mutation and budget gates, current source fingerprint, and production build. Fixture-backed Edge browser checks covered 1920×1080, 1440×900, 1024×768, 768×1024, 390×844, 1365×768 product detail, all 16 current/legacy product-slug route combinations, pause/next, reduced motion, URL/back-forward category and currency state, zero broken images, one page heading, and zero horizontal overflow or final console errors.
- Redrew the shared inline `BoltIcon` path as the correct three-part Third Railify zap, preserving its existing outlined current-color stroke, weight, sizing, and every placement across Public hero, signal, status, newsletter, staging, and not-found surfaces; no supplied or existing SVG/raster asset was substituted or modified.
- Replaced only the Public `/community` hero's Join Discord button icon with the supplied pure-black `discord-0.svg` asset so it reads correctly against the gold primary action; all other Discord icon usages remain unchanged.
- Corrected the Firefox-only sign-in scrollbar regression caused by an unreliable WebKit-selector support probe: Firefox now explicitly retains its original narrow gold-on-dark `thin` scrollbar, while Chromium/Brave keeps the separate five-pixel treatment.
- Made the shared sign-in dialog reliably viewport-bounded in Chromium/Brave as well as Firefox by removing grid-item shrink ambiguity, using dynamic-viewport height with a compatible fallback, and containing vertical scrolling inside the modal so verification and submit actions remain reachable through a minimal five-pixel Chromium scrollbar or Firefox's narrowest supported `thin` treatment.
- Upgraded the Shop, Community, Watch, and About hero surfaces with the homepage's motion-gated signal field, animated gold feature typography, and route-specific product-orbit, community-radar, broadcast-scope, and show-format-map treatments. Existing catalogue, Discord, broadcast, migration-state, responsive, and reduced-motion behavior remains authoritative.
- Corrected Public email sign-in so existing credentials are not blocked by the 12-character new-password policy while signup/reset keep that minimum. Added a bounded same-origin display-name proxy and `/account` editor that require the current Public session and CSRF proof, then delegate the D1 mutation to Admin authority.
- Reordered the shared header actions so the authenticated identity widget occupies the former far-right cart position, compacted the cart to its accessible bag/count control, and removed both desktop and mobile supplemental Watch links whenever the normalized broadcast snapshot has no confirmed `liveNow` candidate.
- Upgraded the authenticated Public account widget from a basic link list to a responsive StreamSuites-reference identity menu with avatar header, four-field details matrix, icon-prefixed destinations, keyboard navigation, and a separated sign-out action in the existing Third Railify visual system.
- Added self-service avatar upload and HTTPS URL import to `/account`. Public forwards the current session and CSRF proof through its same-origin Function to the Admin-owned avatar endpoint; it holds no R2 binding, does not update D1, and never persists `blob:` or `data:` URLs. Admin returns the immutable profile-media URL for normal session hydration.
- Removed the Admin-access reference from the Public account dialog introduction so the login modal describes only the Public identity experience.
- Added the Admin-supplied OAuth provider-state contract to the Public account dialog. Google now remains visible but natively disabled and greyed with migration-status copy while the server gate is disabled; unavailable providers stay hidden, Discord remains usable, and no Google credential is exposed client-side.
- Added the Public half of the shared account milestone: an accessible OAuth-first/email login modal, explicit Turnstile lifecycle, same-origin session hydration/logout, one-time D1 handoff exchange, responsive account widget, and real `/account` plus `/account/login` routes.
- Kept `ThirdRailify-Admin` authoritative for passwords, OAuth callbacks, email delivery, account/role state, and abuse controls. Public owns no credential validation or Admin mutation logic and persists no canonical browser identity.
- Added focused Public handoff/session/CSRF/replay tests, exact auth Function routing, minimal Turnstile/Admin-origin/avatar CSP allowances, safe staging origins, sanitized Public environment examples, and a manual shared-D1 setup guide.
- Refreshed the existing state-backend source fingerprint because its protected input set includes Public Function routing and Wrangler configuration; the Durable Object implementation, SQLite snapshot contract, and legacy-KV read-only boundary are unchanged.
- No live OAuth, Turnstile, Resend, Cloudflare D1 resource, binding, secret, deployment, DNS, Wix, Access, or custom-domain change was performed. The missing real D1 ID remains an explicit activation blocker.
- Replaced active community and Watch Workers KV persistence with one stable SQLite-backed `ThirdRailifyPublicState` Durable Object. Its bounded `snapshots` table has independent `community` and `broadcast` rows plus small versioned migration metadata; no poll history is accumulated and cross-key writes remain isolated.
- Kept `thirdrailify-community` unchanged as a read-only bootstrap source. First initialization validates and seeds only missing rows from `discord:community:snapshot:v1` and `broadcast:current:snapshot:v1`, records migration completion in SQLite, never overwrites newer Durable Object state, and performs no later KV reads.
- Added a production-source KV mutation ban, deterministic 24-hour/30-day exact-zero KV operation simulations, SQLite migration/idempotency/concurrency/isolation tests, public-safe `/api/state-backend` diagnostics, source release/fingerprint parity, and the read-only double-clickable `Verify-Cloudflare-State-Backend.cmd` operator check.
- Added Wrangler configuration for the existing `thirdrailify` Pages project to bind to the dedicated non-public `thirdrailify-public-state` Worker, because Pages Functions cannot host a Durable Object class. Existing signed ingest and public community/watch/thumbnail schemas remain the abstraction boundary.
- Superseded the earlier KV budgeting milestone below: community-only throttling could not eliminate writes after the later Watch publisher reused the namespace. Steady-state ThirdRailify KV PUT/DELETE/LIST/GET operations are now exactly zero after the one-time read-only bootstrap; historical analytics totals are expected to remain until retention ages them out.
- Deployed only this migration to the proven Cloudflare targets, finalized as Worker version `dc463dc2-1c4a-4430-9815-83db50e69148` and Pages production deployment `74ed5bec.thirdrailify.pages.dev`. Read-only live acceptance reports `CURRENT`, both migrated snapshots available, the direct Pages KV binding removed, the existing encrypted ingest secret preserved, and the stable domain unchanged.
- Aligned SQLite freshness checkpoints with producer guarantees after removing the KV constraint: community and inactive broadcast now checkpoint every 600 seconds, live/upcoming broadcast every 150 seconds. Volatile timestamps still deduplicate, but a healthy producer no longer appears stale between old 30-minute KV-era writes.
- Eliminated unconditional Workers KV writes from signed community and watch ingest. Both routes now read the current record, compare deterministic SHA-256 fingerprints over normalized meaningful public state, return compatible HTTP 204 no-op success for unchanged requests, and persist a versioned internal envelope only for semantic changes or bounded freshness checkpoints.
- Community excludes only volatile `generatedAt` and checkpoints unchanged content every 1,800 seconds. Broadcast independently excludes poll/lease timestamps and viewer-count churn, persists all public state/selection/metadata transitions immediately, and checkpoints live/upcoming/inactive state at 150/600/1,800 seconds; the live cadence preserves the existing minimum 180-second safety lease.
- Added safe response diagnostics and write-only logs for persistence reason and per-request KV writes without payloads, members, signatures, or secrets. Unchanged requests are represented by response metadata rather than log spam. Invalid HMAC/replay requests remain zero-write, public GET/thumbnail contracts remain unchanged, and no counter is persisted to KV.
- Corrected the bounded Rumble thumbnail proxy to use the Workers-supported manual redirect mode and continue rejecting every redirect/non-success response, after production tailing proved the prior `redirect: error` option threw before the upstream request could be validated.
- Added deterministic idempotency, semantic-change, checkpoint, security, public-read, and quota-budget coverage. The modeled quiet budget falls from 288 to 96 PUTs/day; representative four-hour and six-hour live days are 186 and 230, with 30-day quiet/busy projections of 2,880/6,900. These deliberately leave account-wide headroom without claiming exclusivity over other namespaces.
- Replaced the `/watch` migration shell with a production-shaped read-only broadcast destination driven by the bot-owned `thirdrailify-broadcast-v1` snapshot: live/latest player, Rumble-first simultaneous-live selection, platform switcher, metadata, schedule, direct links, and truthful loading/delayed/stale/unavailable/no-embed states.
- Added signed `POST /api/watch/ingest`, public `GET /api/watch`, exact schema/URL/freshness validation, the separate `broadcast:current:snapshot:v1` record in the existing KV binding, and a key-bound bounded Rumble thumbnail proxy. The existing HMAC secret is reused; no second KV namespace or secret was introduced.
- Added one visibility-aware `BroadcastProvider`, reusable player/status/metadata/selector components, dynamic header/mobile live state, homepage live/latest CTA and platform markers, and a lazy homepage player. Browser code never scrapes a provider or receives ingest credentials.
- Tightened CSP only for exact YouTube privacy-enhanced/YouTube/Rumble frame and YouTube thumbnail hosts while preserving the existing security directives. Added deterministic Function and browser fixtures covering the full state matrix at 390/768/1440 pixels, reduced motion, platform switching, overflow, and console cleanliness.
- Kept `thirdrailify.com`, Wix, Pages projects, KV resources, bindings, secrets, DNS, custom domains, and deployment untouched. No Public version change was made; this remains additive under pending `0.1.0-alpha.0`.
- Strengthened the shared header's verified-live treatment with a redder chip and gentle reduced-motion-safe pulse. The homepage `Follow the rail.` Rumble and YouTube rows now consume the same normalized `liveNow` platform state as the existing platform rail, so live highlighting and `Live now` copy remain consistent without another poller or provider guess.
- Added three original local WebP feature illustrations to the homepage show-universe cards for `Aboot Nothing`, `Pop Culture Beat Down`, and `News Hangout`, with stable responsive crops, gentle edge falloff, lazy decoding, and preserved card copy hierarchy.

- Added a Vite/React/TypeScript/React Router public application and deterministic npm lockfile.
- Added a tokenized responsive dark/gold design system, shared shell, accessible navigation, branded 404, and route migration shells/aliases.
- Established the seeded American Captain face as the main display/header font, supported by Blinker and Geist Mono.
- Relaxed American Captain headline tracking/line-height and removed faux-heavy rendering so titles retain their condensed character without crowding.
- Built the public landing page with CSS/SVG rail-signal atmosphere and visibility, document, mobile-density, and reduced-motion safeguards.
- Replaced the separate Shawn/Gina hero portrait cards with the single seeded `assets/illustrations/shawn-gina-hero.webp` composition, retaining direct host links and responsive signal/HUD treatment.
- Updated the public hero title from `NEWS. CRIME. CHAOS.` to `NEWS. CULTURE. CHAOS.` to better match the show positioning.
- Built a provider-neutral shop UI over a dated eight-product Wix snapshot with local product imagery, search, facets, sorting, details, explicit states, and a local-only cart with disabled checkout.
- Added Cloudflare Pages `_headers`, `_redirects`, `.node-version`, staging documentation, and noindex policy. No Pages project, deployment, DNS, or domain change was performed.
- Added a durable live Wix audit covering routes, content, socials, VIP/support/legal concepts, 13 category labels, verified product evidence, omissions, and migration risks.
- Added lint, TypeScript, and production-build scripts. Validation evidence belongs in the completion report for this milestone.
- Polished document, cart, and horizontal-filter scrollbars with a narrow graphite/gold treatment adapted from the approved POC.
- Rebuilt the shared header/footer lockup around the exact `assets/logos/boltv2.svg` silhouette, a POC-derived dark square, gradient-gold mask paint, and a larger American Captain title while retaining a filtered-image fallback.
- Replaced the temporary inline favicon with the exact `assets/icons/boltv2.ico` asset.
- Added the slow gold feature-text gradient to `CHAOS.` and the `/shop` hero feature word, with a static reduced-motion treatment.
- Replaced the four `Pick your signal.` initials with full-color Rumble, YouTube, Pilled, and X SVG artwork copied from the approved local StreamSuites Public reference.
- Simplified the browser/document title to `Third Railify™` without the former Daily Podcast suffix.
- Checked the visual-polish milestone responsively at 390px, 768px, and 1440px, including overflow, built assets, reduced motion, and browser-console behavior.
- Switched the shared header/footer lockup from `boltv2.svg` to the exact user-supplied `boltv2straight.svg` silhouette while preserving the established gold-gradient mask, dark rounded square, inset edge, hover polish, responsive sizing, and fallback image.
- Replaced the active Public favicon with the exact user-supplied `assets/icons/thirdfavstraight.ico`; previous bolt assets remain preserved but inactive.
- Added a reusable React Discord community system with one validated fetch boundary, bounded eight-second unauthenticated requests, exact guild validation, safe invite/avatar allowlists, compact/full modes, manual refresh, and truthful loading, ready/empty, and error states.
- Integrated the compact live Discord view into the existing goat-led homepage community composition and replaced the `/community` migration shell with a substantial Third Railify Community page using the full live widget.
- Completed the `Follow the rail.` platform artwork with exact local TikTok and Discord SVG copies while preserving all established destination URLs, including the source-spelled TikTok handle.
- Validated the milestone with Public lint, TypeScript, production build, diff checks, live/manual-refresh Discord requests, deterministic loading/empty/error states, exact built-favicon bytes and content type, and rendered 390px/768px/1440px home and Community checks with no overflow, broken images, or normal-path console errors.
- Corrected the shipped Pages Content Security Policy so the live Discord widget may connect to `https://discord.com` and its validated public avatars may load from `cdn.discordapp.com` or `media.discordapp.net`; all other connection and remote-image sources remain denied. Wrangler Pages local validation confirmed live homepage/full-page data, avatar loading, manual Refresh, and zero CSP or application-console errors under the emitted production headers.
- Aligned the homepage and Community Discord directories at 12 public member presences by default and added the same accessible Show more/Show fewer control to both, with a strict rendered maximum of 24 and truthful returned-presence progress copy.
- Added same-origin Cloudflare Pages Functions for signed `POST /api/community/discord/ingest` and public `GET /api/community/discord`, one `THIRDRAILIFY_COMMUNITY_KV` binding, the versioned `discord:community:snapshot:v1` key, normalized-field-only persistence, and server-derived fresh/delayed/stale metadata.
- Added HMAC-SHA256 verification over `<timestamp>.<raw body>`, a 300-second replay window, a 96 KiB request limit, exact guild/schema/type/count/URL checks, safe no-store responses, and focused local Function tests using test-only secrets.
- Added `public/_routes.json` so only `/api/community/*` invokes Pages Functions while `_redirects`, static Vite output, direct-load SPA behavior, and staging noindex/security headers remain intact.
- Upgraded both Discord widgets to prefer the enriched bot snapshot, fall back truthfully to the existing basic Discord public widget, and show an intentional unavailable state only when both sources fail.
- Added a bounded whitelisted public-channel directory, fresh/delayed/stale copy, stale-presence neutralization, and Third Railify-styled member profile cards supporting hover preview, keyboard focus, click/tap pinning, Escape/outside close, viewport collision handling, and reduced motion.
- Preserved 12 collapsed and 24 expanded member limits. Compact homepage mode shows fewer public/voice channels; full `/community` mode shows the larger bounded directory without changing routes or surrounding layouts.
- Increased the compact homepage community-channel display from three to four, reduced the full `/community` default to six, and added a responsive, keyboard-accessible Show all/Show fewer control that exposes every sanitized public channel returned by the existing API contract.
- Expanded Cloudflare setup documentation with manual KV/secret/bot environment steps. No Cloudflare resource, secret, deployment, DNS, Wix, custom-domain, commit, or push change was performed.
- Aligned the existing Discord GET freshness contract with the bounded bot cadence: fresh below 720 seconds, delayed from 720 through 1199 seconds, and stale from 1200 seconds, with exact boundary tests and unchanged stale-presence neutralization.
- Documented the bridge's Free-tier-conscious five-minute automatic floor, semantic deduplication, and ten-minute unchanged heartbeat without changing the KV binding/key, API routes, payload schema, HMAC secret/algorithm/replay window, sanitization, browser loading behavior, or Discord widget UX.
- Excluded generated `.wrangler` output from the flat ESLint input set so local Pages tooling artifacts no longer masquerade as application lint failures.
- Removed the compact two-item and full eight-item Voice/Stage presentation caps so both modes render every bounded `voiceSpaces` entry returned by the existing snapshot. Community channels now derive only text, announcement, and forum entries while Voice spaces derives only voice and stage entries; the overview total still represents the complete configured public directory.
- Refined channel-card semantics and spacing so dynamic Discord topics sit on their own readable two-line description row, with category names retained only as tertiary metadata in full mode. Voice/Stage cards remain shorter and never render topic placeholders. Compact audio overflow now activates only beyond a 236px panel bound using the existing graphite/gold scrollbar treatment; deterministic 390px, 768px, and 1440px browser checks covered six audio entries, Stage reachability, a four-entry no-scroll fit, full 6-to-8 Community expansion, wrapping, focus, partitioning, and horizontal overflow.
- Replaced the active Public-route favicon with the user-supplied `assets/icons/thirdfav3.ico`; the shared SPA document applies it across every Public page without changing Admin branding or the preserved prior favicon assets.
- Scoped the red outer `LIVE NOW` chip pulse to the shared header and added a restrained Rumble-lime live-state pulse to the currently displayed homepage and Watch player cards. Candidate-specific state keeps archive selections on their established static presentation, while reduced-motion users retain static signal styling.
- Prefixed shared Rumble broadcast actions with the existing Rumble SVG, eased the homepage hero portrait into view on load, and feathered the left edge of its right-side signal field so the established midpoint start no longer appears as a hard vertical boundary.
- Routed live entries in both homepage social rails to the validated broadcast candidate's direct `watchUrl`; non-live entries continue using their established platform profile destinations.
- Reframed the homepage host cards and `/shawn` and `/gina` route cards with near-square `6:5` image areas using the supplied `shawn1x.webp` and `gina1x.webp` portraits, with a gentle bottom falloff into the adjoining content. Removed the homepage corner numbers so Shawn and Gina are presented by name rather than as numbered entries.

### Human-readable

GOATS in the Wild is now a real V2 community experience in the local staging code: people can browse approved stories on a proper in-page map and gallery, open a polished detail page, and submit their own product-linked photos for review. The real gallery deliberately begins empty until the Wix export arrives; two clearly disposable examples are available only when an operator opts into the Admin test seed.

Private submissions do not become public automatically. Photos and email remain behind the Admin boundary, locations are city-level, and signed-in members can react or comment only on approved, visible listings.

The shop now feels like a proper Third Railify merch drop instead of a migration worksheet: the hero cycles through the products chosen in Admin, collections lead with real product art, the catalogue is faster to explore, and every product page carries the same polished presentation. CAD stays the real price everywhere, with an optional approximate display conversion that can never block shopping or alter the local cart.

Existing account passwords now work at sign-in even when they predate the current new-password length rule. The account page also lets signed-in visitors update their display name alongside their avatar.

The account control now sits at the far-right edge of the main header actions, the cart takes less space, and the extra live-watch signal appears only while a broadcast is actually confirmed live.

Signed-in visitors now get a much richer account menu and can change their avatar from the account page using a JPG, PNG, WebP, or public image URL. The image is stored by the Admin account service behind a clean media path rather than embedded into browser/account state.

The Public header now has a real Third Railify account control and `/account` destination. Sign-in is designed to use the shared Admin-owned account service without putting credentials, provider secrets, or permanent identity state in the browser.

Third Railify V2 now has its first real public-site foundation: a cinematic show homepage and a shop that uses only a small, clearly labelled slice of the current catalogue. Visitors can explore products and try the cart interaction, but nothing takes payment and no private or provider system is connected. The site is ready for a separate staging review while Wix remains untouched in production.

The Discord directory now keeps conversation channels and live audio spaces distinct: Discord-authored descriptions are easier to scan, while every published Voice and Stage space remains reachable without stretching the homepage indefinitely.

Every Public page now uses the supplied `thirdfav3.ico` browser icon.

Verified live broadcasts now carry the same red live signal in the header and both homepage platform lists.

Live player cards now carry a gentle green signal on Home and Watch, Rumble actions include their platform mark, and the homepage portrait arrives with a softer edge and entrance.

When a listed provider is live, both homepage social rails now open that confirmed stream directly; otherwise they continue opening the provider profile.

### Known deferrals

- Complete Wix catalogue and collection mapping.
- Printful/Printify APIs, server-side commerce, checkout, orders, tax, shipping, and inventory.
- Live account infrastructure/configuration acceptance, VIP, donations, gift cards, newsletter delivery, CMS, and approved legal copy.
- Cloudflare project creation, deployment, custom domains, DNS, analytics, and production cutover.

## 2026-08-29 — Canadian legal release gate hardening

- Added the internal privacy operations runbook, contact-role matrix, complete retention inventory, third-party data-flow dossier, and Ontario checkout release-gate matrix. These documents distinguish implemented evidence from owner, legal, and provider-contract authority and do not claim legal compliance.
- Normalized `LEGAL_RELEASE_CHECKLIST.md` to 0 current engineering-owned blockers, 8 owner-decision matters, 6 qualified legal-review matters, and 1 provider/external-review matter that is also included in the legal count.
- Added focused policy/release tests for owner-safe London, Ontario presentation, Canadian-only wording, approved contact aliases, no invented entity/registration/age/return/representative facts, CAD authority, qualified conversions, consent schema 1, disabled checkout, and the non-operational disabled newsletter.
- Public policy runtime copy did not require correction: it already uses neutral `privacy contact` language, preserves statutory remedies without inventing a return window, keeps checkout disabled, and names Shawn without a surname.

### Remaining owner/legal handoff

- Owner decisions remain for contracting form/address, active customer-facing contact routes, privacy accountability, retention, minors, sales footprint, voluntary change-of-mind and remedies authority.
- Qualified review remains for Ontario/PIPEDA scope, tax/business identifiers, request/incident procedures, provider/international processing, final checkout, and EU/UK scope/representatives.

## 2026-08-29 — mobile Public hero typography

- Normalized Public H1 leading at tablet and phone breakpoints so the American Captain display glyphs no longer overlap or clip between stacked lines.
- Applied the fix through the shared Public content boundary so every current hero, detail, loading, empty, account, cart, policy, commerce, Watch, GOATS, Community, and Donate page inherits the same mobile rhythm without changing desktop presentation.
- Added a focused browser audit covering every distinct Public page route at 768px, 390px, and the 360px edge breakpoint, including line-box separation, eyebrow clearance, horizontal overflow, and the loaded display font.

## 2026-08-29 — dedicated About story

- Replaced the `/about` migration shell, old-site handoff, placeholder checklist, and unfinished format map with a dedicated source-guided editorial page. The supplied owner story was condensed and rearranged into a high-voltage opening, an origin chapter, a Shawn/Gina dynamic, recurring formats, the live community circuit, and a manifesto close without adding history, metrics, testimonials, fake chat, surnames, or unsupported role assignments.
- Built a bespoke motion-gated CSS/SVG hero in which NEWS, CULTURE, CHAOS, and CHAT feed a central Third Railify junction through bounded travelling pulses, orbit paths, a waveform, a voltage meter, and a live rail. The same layout remains complete and polished with reduced motion, and below-fold format/community motion pauses outside the viewport.
- Added first-party lazy-loaded Shawn and Gina portrait panels, four distinct modules for Aboot Nothing, Pop Culture Beat Down, News Hangout, and the wildcard, plus abstract community and closing treatments. Viewing stays on the internal `/watch` route; no redundant contact block, email list, external platform directory, remote asset, API request, or new dependency was added.
- Removed About-specific content, branches, diagram markup, types, and styles from `RouteShellPage` while preserving the then-current host, Friends, VIP, and gift-card shell behaviour. Added focused source/content/absence/structure/animation/reduced-motion/image/console/overflow browser regression coverage across 1920×1080, 1440×900, 1365×768, 1024×768, 768×1024, and 390×844.
- Staging deployment state: deployed only to the existing Public Pages project as `f52ca89b-f5c1-4a23-a072-bc5d8976bba8` (`https://f52ca89b.thirdrailify.pages.dev`). Stable `/about` desktop/mobile acceptance confirmed HTTP 200, the finished motion system and copy, loaded first-party portraits, no overflow, no page-origin console errors, complete reduced-motion content, and clean representative Home, Watch, Shop, GOATS, Shawn, and Gina routes. No Admin, data, provider, Wix, DNS, domain, custom-domain, or production-cutover change was performed.

## 2026-08-29 — dedicated Shawn and Gina host stories

- Replaced both `/shawn` and `/gina` migration shells with dedicated editorial host stories driven by one typed profile structure and two deliberately different treatments: Shawn's gold/cyan broadcast console and Gina's pink/gold curiosity circuit. Each page now has a bespoke portrait hero, voice profile, four topic instruments, cross-host chemistry, and an internal Watch/community close.
- Kept the copy within first-party repository evidence: Shawn is presented as the Canadian Third Railify host covering news, crime, pop culture, and ADHD-shaped detours; Gina is presented as the Canadian Third Railify co-host bringing sass, smarts, humour, mysteries, culture, and a distinct Just Gina lane. No surname, invented biography, metrics, testimonials, fake chat, unstable standalone schedule, platform directory, contact block, external asset, API request, or new dependency was added.
- Removed all Shawn/Gina content, portrait branches, and accent handling from `RouteShellPage` while preserving Friends, VIP, and gift-card shell behaviour. Added focused source/content/absence/structure/animation/reduced-motion/image/console/overflow browser regression coverage across 1920×1080, 1440×900, 1365×768, 1024×768, 768×1024, and 390×844.
- Staging deployment state: deployed only to the existing Public Pages project as `e992670b-2bf3-4bfa-8b06-08abc698cd6a` (`https://e992670b.thirdrailify.pages.dev`). Stable `/shawn` and `/gina` desktop/mobile acceptance confirmed HTTP 200, complete editorial content, active and reduced-motion-safe visuals, loaded first-party portraits, no overflow, no page-origin console errors, and clean representative About, Watch, Shop, GOATS, and Friends routes. The immutable routes and exact built asset also return HTTP 200; its expected Admin auth-config CORS boundary remains unchanged because only the stable Public origin is allowlisted. No Admin, data, provider, Wix, DNS, domain, custom-domain, or production-cutover change was performed.
- Follow-up visual correction: moved the circular MIC zap from the centre of both hero portraits into the upper-left instrumentation corner and lowered the adjacent input chip, keeping both host faces unobstructed at every supported viewport. Focused browser geometry now enforces that safe corner placement. Deployed only to the Public Pages project as `bdc0f345-ad34-48ba-a5fe-4f5c3f7a421f` (`https://bdc0f345.thirdrailify.pages.dev`); stable desktop/mobile screenshots and the full remote host suite confirm the corrected placement.

## 2026-08-29 — live Watch event perimeter

- Rebuilt the primary Watch stage's authoritative live-only treatment as a conspicuous crimson-and-gold perimeter with a breathing multi-layer glow, pulsing edge brackets, and an outer halo aligned with the automatic Live Now banner language.
- Kept archived, upcoming, stale, and unavailable transmissions free of the live treatment. Reduced-motion visitors receive the same strong live boundary without animation, and forced-colour mode retains a clear system-highlight outline.
- Added desktop and mobile browser assertions for the live class, all three animation layers, offline exclusion, reduced-motion behavior, overflow safety, and visual screenshots.

## 2026-08-29 — bounded Live Now lease grace

- Added a two-minute grace after the bot's last positive `liveExpiresAt` lease so small provider-check or publish timing gaps no longer briefly remove the Public Live Now banner, header indicator, Watch hero, or live-stage treatment. The Public API and browser now use the same boundary.
- Preserved authority and fail-closed limits: a newly ingested authoritative offline snapshot clears live immediately, and a snapshot reaching the existing 15-minute stale threshold can never claim live. The grace does not fabricate a live candidate, extend archive history, change bot/provider writes, or mask an explicit negative check.
- Added exact boundary coverage for 119 seconds retained versus 120 seconds expired, immediate offline clearing, client banner/header/Watch continuity inside the grace, fallback beyond it, and the full responsive Watch browser matrix.
- Staging deployment state: deployed from an isolated release worktree only to the existing Public Pages project as `4b623f4a-e544-4778-8e41-337ce0a2edf6` (`https://4b623f4a.thirdrailify.pages.dev`), excluding concurrent SEO/runtime edits in the shared worktree. Stable acceptance observed 18 consecutive live API samples across multiple real bot lease cycles with no live-count drop, including 2 seconds before a lease refresh; a real browser simultaneously confirmed the live banner, header indicator, Watch hero, and live stage with HTTP 200, no overflow, and no page errors.

## 2026-08-29 — route-wide SEO and social previews

- Replaced the generic, globally noindexed document metadata with one shared SEO authority covering every canonical Public route. Each indexable page now has a distinct title, description, canonical URL, Open Graph/Twitter large-card metadata, appropriate first-party social artwork, and schema.org JSON-LD; account, cart, checkout-result, submission, missing, unknown, and immutable-preview routes remain noindex.
- Added root Pages middleware so Discord, X, search crawlers, and other unfurlers receive route-specific metadata in the initial HTML rather than waiting for React. Dynamic product, Watch episode, and approved GOATS detail previews reuse the existing sanitized Public read projections and fail softly without provider calls or new storage.
- Added generated `/robots.txt` and `/sitemap.xml`, canonical edge redirects for legacy aliases, stable first-party `/social/*` assets, client-navigation metadata synchronization, an allowlisted presentation-only override seam for later Admin management, focused unit/browser coverage, and local Cloudflare Pages runtime acceptance. No Admin, Wix, provider, commerce authority, checkout, payment, fulfillment, DNS, or deployment state was changed.
- Follow-up route audit fixed the post-release Wheels gap: `/wheels`, creation, public detail, protected editor, and presentation routes now publish deliberate titles, descriptions, canonicals, crawl policy, social metadata, and structured data instead of inheriting the 404 client title or generic home HTML. Public wheel details use the sanitized wheel projection, active public wheels are included in the sitemap, `/wheel` redirects canonically, and the SEO command now runs both route-authority and browser-navigation coverage.
- Follow-up staging deployment: `1bd4db54` (`https://1bd4db54.thirdrailify.pages.dev`) on the existing Public Pages project. Stable HTTP and real-browser acceptance confirmed correct metadata for the Wheels directory and demo detail/presentation routes, a legitimate 404 only for missing routes, successful 404-to-Wheels client navigation without a stale title, sitemap discovery, and zero page errors. The isolated release excluded concurrent Wheels UI work and made no Admin, provider, payment, fulfillment, Wix, DNS, or production-domain change.

## 2026-08-29 — Friends of the show ensemble page

- Replaced the `/friends` Wix migration shell with a dedicated, first-party ensemble story for Daniel Clancy, Darnell Quiggley, and Simple Davy using the three supplied local portraits and owner-supplied appearance/personality details.
- Added a motion-gated three-signal hero, individual high-impact dossier cards, a closing internal Watch/About path, and reduced-motion/forced-colour treatments. No remote imagery, API dependency, metrics, history, testimonials, or separate profile routes were added.
- Kept every supplied Rumble, YouTube, and X destination out of the summary cards. Channel and social links appear only after a profile opens in its keyboard-contained, Escape-dismissable modal, with focus restoration and safe external-link attributes.
- Added focused source, content, social-visibility, dialog accessibility, animation, reduced-motion, image, console, and overflow browser coverage across the full Public viewport matrix.
- Staging deployment state: deployed from an isolated release worktree only to the existing Public Pages project as `ce76a13a-33cd-4e5c-aac6-2d5d7710076b` (`https://ce76a13a.thirdrailify.pages.dev`), excluding concurrent SEO/runtime edits in the shared worktree. Stable desktop/mobile browser acceptance confirmed the completed hero, all three cards, dialog-only social links, keyboard behavior, loaded first-party imagery, active and reduced-motion-safe visuals, no horizontal overflow, and no page-origin console errors.
- Follow-up dialog polish adds the existing first-party Rumble, YouTube, and X SVG marks as leading button icons and applies profile-specific portrait crops so Daniel, Darnell, and Davy fill their modal canvases without changing the link-only-inside-dialog boundary.
- Follow-up composition polish replaces the page-wide coral/red feature system with electric violet, gives Davy a distinct brighter purple profile accent, enlarges all hero/card cutouts to use their canvases, and moves Daniel to the front-centre hero lane so every regular remains visible.
- Final follow-up deployment: `eea51cc4-95a0-477b-bcc2-cc355944b1bb` (`https://eea51cc4.thirdrailify.pages.dev`) on the existing Public Pages staging project. Stable desktop/mobile screenshots and the complete remote Friends browser suite confirm the violet palette, Daniel-front hero hierarchy, full-canvas portraits, SVG-prefixed dialog links, reduced-motion behavior, and zero horizontal overflow or page errors.
- Final hero framing refinement enlarges Daniel's front-centre cutout and nudges its anchor left while preserving Darnell and Davy's established background lanes.
- Hero framing refinement deployed as `85ef55fd-4e32-4979-9609-81bb257e29b8` (`https://85ef55fd.thirdrailify.pages.dev`); the stable six-viewport suite confirms Daniel's larger left-of-centre foreground geometry, all three visible portraits, and unchanged dialog/link accessibility.
- Hero floor correction pins all three cutout wrappers to the instrument floor, removes vertical translation from their idle animation, moves Daniel farther left, and raises Darnell through bottom-anchored scaling so no portrait can expose a floating gap beneath it.
- Card framing correction replaces the three dossier portraits' `contain` geometry with top-anchored full-bleed cover crops, explicitly bypasses the global image-width cap, and shifts Daniel's oversized layer left. The portraits now extend beyond both canvas edges without cropping their top edge at desktop, tablet, or phone widths; focused geometry assertions and fresh 1440/390 screenshots cover the result.

## 2026-08-29 — shared current-live player perimeter

- Extended the existing authoritative Watch-page crimson-and-gold live perimeter to the homepage current player and the dedicated `/watch/live` player, including the breathing stage, edge-bracket, and halo layers.
- Applied the treatment only to candidates accepted by the shared bounded current-live authority; offline, stale, expired, archived, and fallback presentations remain unlit. Reduced-motion and forced-colour behavior remain intact.
- Removed Wheels from the desktop and mobile main header only. The `/wheels` route and the Community-page entry remain available, and mobile navigation numbering now follows the visible header items.
- Expanded the focused Watch browser suite across live, offline, stale, reduced-motion, 1440px, and 390px states, with header/Community navigation assertions and fresh visual captures for the homepage and dedicated live player.

## 2026-08-29 - Wheels V1.5 modal containment and white-slice labels

- Removed the random Wheels editor/Appearance dots at their source: the 12 intentional wheel-rim markers had a hard-coded 333 px transform radius that escaped compact previews and enlarged scrollable modal layout. They now use percentage positions inside the clipped, non-interactive, `aria-hidden` rim; no blanket page overflow workaround was used.
- Rebuilt Wheel Editor and Appearance around fixed header/tab/footer regions plus bounded vertical-only content scrollers. Added `min-width: 0`, flexible `minmax(0, 1fr)` tracks, bounded controls, tablet participant-row collapse, and long import/conversion wrapping across Wheel Editor, Appearance, Participant Manager, Import / Export, and participant details.
- Improved Canvas label legibility only for near-white slices by switching those labels to dark ink with a light shadow. Existing light labels on gold, crimson, violet, and other darker colours remain unchanged.
- Added the Wheels V1.5 static suite and expanded the real browser matrix with per-surface `scrollWidth`/`clientWidth`, child-bound, decoration-containment, idle-presentation-particle, console, responsive, and screenshot assertions. Public-only Pages deployment and stable remote acceptance are recorded after release validation; no Admin, API contract, schema, migration, D1/R2, Wix, DNS, domain, checkout, provider, or official-spin change is part of V1.5.
- Public release: `23f28c9f-5d9c-4951-84bf-8dc714afc1c5` (`https://23f28c9f.thirdrailify.pages.dev`). The stable alias serves the same entry assets; direct Wheels routes return HTTP 200, deployed V1.1-V1.5 suites pass without official/API writes, and opened stable 1440/390 screenshots confirm dark white-slice labels, contained rim lights, no random dots, and no horizontal overflow. Protected creator dialogs remain remotely unbypassed and are covered by the authenticated local browser matrix.
