export type WheelEntry = { id: string; label: string; order: number; weight: number; colour: string | null; state: "active" | "hidden" };
export type WheelThemePreset = "third-rail-gold" | "live-wire-red" | "gina-violet" | "high-voltage-mono" | "signal-teal" | "after-hours";
export type WheelConfig = { themePreset: WheelThemePreset; palette: string[]; pointerAccent: string; centreTreatment: "bolt" | "signal" | "ring"; backgroundIntensity: "low" | "medium" | "high"; labelContrast: "light" | "dark"; spinDurationMs: number; tickingSoundEnabled: boolean; winnerSoundEnabled: boolean; celebrationEnabled: boolean; confettiEnabled: boolean; winnerLightingEnabled: boolean; celebrationIntensity: "subtle" | "normal" | "strong"; backgroundEnabled: boolean; backgroundFocalX: number; backgroundFocalY: number; backgroundImageOpacity: number; backgroundOverlayIntensity: number; winnerMessageTemplate: string; publicHistoryVisible: boolean };
export type WheelMediaAsset = { id: string; purpose: "background" | "centre"; url: string; contentType: string; byteSize: number; width: number | null; height: number | null; sha256: string; createdAt: string };
export type OfficialResult = { id: string; type: "official"; winningEntryId: string; winningLabel: string; winningWeight: number; wheelRevision: number; snapshotHash: string; createdAt: string; voided: boolean };
export type WheelAccess = { role: "owner" | "editor" | "spinner" | null; isMasterAdmin: boolean; canEdit: boolean; canSpinOfficially: boolean; editingLocked: boolean; officialSpinLocked: boolean; revision?: number };
export type Wheel = { slug: string; title: string; description: string | null; lifecycle: "draft" | "active" | "archived"; visibility: "public" | "hidden"; participantCount: number; weighted: boolean; entries: WheelEntry[]; config: WheelConfig; media: { background: WheelMediaAsset | null; centre: WheelMediaAsset | null }; demoEnabled: boolean; officialEnabled: boolean; latestOfficialResult: OfficialResult | null; recentOfficialResults: OfficialResult[]; revision?: number };
export type WheelSummary = { slug: string; title: string; description: string | null; participantCount: number; weighted: boolean; themePreset: string; palette: string[]; demoEnabled: boolean; officialEnabled: boolean; latestOfficialAt: string | null };
