export type PollState = "draft" | "open" | "closed" | "archived";
export type PollMediaAsset = { id: string; purpose: "banner" | "option"; optionId?: string | null; url: string; contentType: string; byteSize: number; width: number; height: number; fileName?: string; createdAt: string };
export type PollOption = { id: string; position: number; label: string; description: string | null; trigger: string; normalizedTrigger: string; votes: number; image?: PollMediaAsset | null };
export type Poll = { id: string; slug: string; title: string; description: string | null; state: PollState; public: boolean; webVotingMode: "anyone" | "signed_in"; rumbleEnabled: boolean; rumbleSourceScope: string | null; livestreamMode?: "automatic" | "exact"; livestreamId?: string | null; requestedIntervalSeconds?: number; revision: number; totalVotes: number; options: PollOption[]; owner: { id: string; displayName: string; avatarUrl: string | null; adminLevel?: string | null }; updatedAt: string; openedAt: string | null; closedAt: string | null; currentVoteOptionId?: string | null; theme?: { accent?: string; layout?: string }; media?: { banner?: PollMediaAsset | null } };
export type PollAccess = { canManage: boolean; canManageAll: boolean; isOwner: boolean };
export type RumbleDiscovery = { ok: true; provider: "rumble"; botState: "healthy" | "stale" | "offline"; freshness: { heartbeatAt: string; ageSeconds: number; providerResponseAt: string | null; observedAt: string | null } | null; source: { scope: string; type: "user" | "channel"; id: string; displayName: string } | null; livestreams: Array<{ id: string; title: string; isLive: boolean; createdOn: string | null; scheduledOn: string | null; visibility: string | null; watchingNow: number | null }>; message: string | null };
